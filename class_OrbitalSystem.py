"""

"""
import class_OrbitingBody

def OrbitalSystem(self, list_of_bodies):
    """
    
    """

    def save_all_bodies_dx_dy_variations(list_of_bodies):
        """
        
        """
        for i, body in enumerate(list_of_bodies):
            for i, body in enumerate(list_of_bodies):
                self.posicao_x = self.posicao_x + self.variation_dx(list_of_bodies)
                self.posicao_y = self.posicao_y + self.variation_dy(list_of_bodies)


    def update_all_bodies_coordenates():
        """
        
        """
        for 